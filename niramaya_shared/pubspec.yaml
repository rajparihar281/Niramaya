name: niramaya_shared
description: Shared models and services for Niramaya apps.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  flutter_riverpod: ^2.5.1
  dio: ^5.4.3
  geolocator: ^12.0.0
  latlong2: ^0.9.0
  flutter_tts: ^4.0.2
  flutter_foreground_task: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
